# 学工处数据库系统的开发
```
1、数据类型和查询格式

数据来源应该是孙老师——普通学生助理是没有本地保存数据的：登陆服务器操作？？

2、标准元数据转换

应该是xlsx -> csv

通过csv导入后写入数据库吧

10.1 修改：可以直接使用xlsx来修改，但是要做检测数据表信息的模块确保应对能力

3、增删查改权限设置

暂时就root吧

4、可视化

前端界面

暂时先用人家写的单页面做测试

可能的话，用vue.js写写看。

5、部署服务器
//暂时这些

```

10.9 号更新

1、数据库表生成办法

先扫描上传（导入）的表，测试是否和学生信息表产生冲突，然后各自形成自己的表格——在命名上，通过字典对应的方式扫描表名或者表内容形成英文可读表名，方便Django生成Model。

主要出现的意外表可能有第一行或前两行是表名而不是对象名

解决思路是扫描前五行，找到“姓名”or“学号”则直接返回开始扫描下边的内容

相对于每个大项直接创建一个表，共生成19+18张表，然后建一张学生信息表，每次写入前表时写入学生信息，如果查询某学生共发放款项可以通过该索引直接去前表中获取信息，提高效率

//作为网站的一个新的app 和查询界面分开，但是页面还是一个页面？

2、前后端交互

后端写成restful api供前端直接ajax请求读取就行。

//前端目前是委托大创实验室李治远同学做

3、部署服务器

已经在废旧电脑上使用花生壳服务做过了，直接挂上去应该不难。——数据量也不大

考虑下阿里云的海外服务器吧···



